services:
  app:
    build: .
    ports:
      - "80:8080"
    volumes:
      - ./appsettings.json:/app/appsettings.json
      - ./uploads:/app/uploads
      - ./favicon.ico:/app/wwwroot/favicon.ico
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
